<x-layouts.app title="Trattoria · prenota un tavolo">
    <section class="section"><div class="container"><p class="eyebrow">Prenotazione tavolo</p><h1>Il tuo posto è qui.</h1><p class="lead">Compila il modulo e indicaci come contattarti. La richiesta verrà presa in carico dal team.</p><div class="form-card" style="margin-top:2rem"><form method="post" action="{{ url('/prenota') }}" data-contract="restaurant.reservations.create"><div class="form-grid">@csrf<div class="field"><label for="customer_name">Nome e cognome</label><input id="customer_name" name="customer_name" required autocomplete="name"></div><div class="field"><label for="phone">Telefono</label><input id="phone" name="phone" type="tel" required autocomplete="tel"></div><div class="field"><label for="email">Email</label><input id="email" name="email" type="email" autocomplete="email"></div><div class="field"><label for="party_size">Persone</label><select id="party_size" name="party_size" required>@for($size=1;$size<=12;$size++)<option value="{{ $size }}">{{ $size }} {{ $size === 1 ? 'persona' : 'persone' }}</option>@endfor</select></div><div class="field"><label for="reservation_date">Data</label><input id="reservation_date" name="reservation_date" type="date" min="{{ now()->format('Y-m-d') }}" required></div><div class="field"><label for="reservation_time">Orario</label><input id="reservation_time" name="reservation_time" type="time" required></div><div class="field full"><label for="notes">Note</label><textarea id="notes" name="notes" placeholder="Allergie, esigenze o richieste speciali"></textarea></div></div><p class="hint" style="margin:1rem 0">La prenotazione viene registrata come <strong>confirmed</strong> nel modello Restaurant\Models\Reservation, quando la route pubblica sarà collegata all'action di dominio.</p><button class="button" type="submit">Invia richiesta</button></form></div></div></section>
</x-layouts.app>
